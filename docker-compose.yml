services:
  backend:
    build: ./backend
    restart: unless-stopped
    env_file: .env
    expose:
      - "8000"

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
